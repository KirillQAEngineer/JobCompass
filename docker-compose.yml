services:
  api:
    build:
      context: .
      dockerfile: docker/api/Dockerfile

    container_name: careerpilot-api

    ports:
      - "8000:8000"

    volumes:
      - ./apps/api:/app

    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload
